# Comando para correr el phpAdmin de mySql
php -S localhost:8080 -t /opt/homebrew/share/phpmyadmin

# Comando para correr la aplicación en el puerto 8000
php artisan serve --host=localhost --port=8000
